┌─────────────────────────────────────────────────────────┐
│                      前端 (Vue3)                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────────┐   │
│  │ 工单创建  │  │ 工单列表  │  │ 工单详情+AI回复建议   │   │
│  └──────────┘  └──────────┘  └──────────────────────┘   │
└────────────────────────┬────────────────────────────────┘
                         │ HTTP
┌────────────────────────▼────────────────────────────────┐
│                   后端 (Spring Boot)                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────────┐   │
│  │工单Controller│ │AI Controller│ │ 知识库Controller    │   │
│  └──────────┘  └──────────┘  └──────────────────────┘   │
│  ┌──────────────────────────────────────────────────┐   │
│  │              TicketService (业务层)                │   │
│  └──────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────┐   │
│  │           AIServiceClient (AI调用客户端)           │   │
│  └──────────────────────────────────────────────────┘   │
└────────────────────────┬────────────────────────────────┘
                         │ HTTP
┌────────────────────────▼────────────────────────────────┐
│                   AI 服务 (Python FastAPI)                │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────────┐   │
│  │ 分类服务   │  │ RAG服务   │  │ 回复建议服务          │   │
│  └──────────┘  └──────────┘  └──────────────────────┘   │
│  ┌──────────────────────────────────────────────────┐   │
│  │              LangChain 编排层                      │   │
│  └──────────────────────────────────────────────────┘   │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│                    数据存储层                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────────┐   │
│  │ MySQL    │  │ 向量数据库 │  │ 本地文件/知识库        │   │
│  │ (工单数据) │  │ (FAISS/Milvus)│  │ (Markdown/PDF)     │   │
│  └──────────┘  └──────────┘  └──────────────────────┘   │
└─────────────────────────────────────────────────────────┘


用户创建工单
    │
    ▼
┌─────────────────────────┐
│  Spring Boot 后端        │
│  1. 接收工单数据          │
│  2. 异步触发 AI 分类      │
│  3. 返回工单创建成功      │
└────────┬────────────────┘
         │ 异步调用
         ▼
┌─────────────────────────┐
│  Python AI 服务          │
│  ┌───────────────────┐  │
│  │ Prompt 模板        │  │
│  │ "根据以下工单内容， │  │
│  │  判断：            │  │
│  │  1. 问题类型       │  │
│  │  2. 优先级         │  │
│  │  3. 用户情绪       │  │
│  │  返回 JSON 格式"   │  │
│  └───────────────────┘  │
│         │                │
│         ▼                │
│  ┌───────────────────┐  │
│  │ ChatTongyi (LLM)  │  │
│  │ 返回:              │  │
│  │ {                  │  │
│  │   "category": "...",│ │
│  │   "priority": "...",│ │
│  │   "emotion": "..." │  │
│  │ }                  │  │
│  └───────────────────┘  │
└────────┬────────────────┘
         │ 回调更新
         ▼
┌─────────────────────────┐
│  Spring Boot 后端        │
│  更新工单的 category、    │
│  priority、emotion 字段  │
└─────────────────────────┘

客服打开工单，点击"AI建议"
    │
    ▼
┌─────────────────────────┐
│  Spring Boot 后端        │
│  1. 获取工单详情          │
│  2. 获取工单历史回复      │
│  3. 调用 AI 服务          │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│  Python AI 服务          │
│  ┌───────────────────┐  │
│  │ RAG 检索链          │  │
│  │ 1. 工单内容向量化   │  │
│  │ 2. 检索相似知识库   │  │
│  │ 3. 获取相关文档     │  │
│  └────────┬──────────┘  │
│           │              │
│           ▼              │
│  ┌───────────────────┐  │
│  │ Prompt 模板        │  │
│  │ "你是客服专家，     │  │
│  │  根据以下信息：     │  │
│  │  - 工单内容        │  │
│  │  - 历史对话        │  │
│  │  - 相关知识库文档   │  │
│  │  生成回复建议"     │  │
│  └────────┬──────────┘  │
│           │              │
│           ▼              │
│  ┌───────────────────┐  │
│  │ ChatTongyi (LLM)  │  │
│  │ 返回回复建议文本    │  │
│  └───────────────────┘  │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│  Spring Boot 后端        │
│  返回 AI 建议给前端       │
│  前端展示在回复框上方     │
└─────────────────────────┘

┌─────────────────────────────────────────────────┐
│                    RAG 架构                       │
│                                                   │
│  ┌─────────────┐                                 │
│  │ 知识库文档    │  (Markdown/PDF/TXT)             │
│  │ - 产品手册    │                                 │
│  │ - 常见问题    │                                 │
│  │ - 操作指南    │                                 │
│  └──────┬──────┘                                 │
│         │ 离线处理                                 │
│         ▼                                         │
│  ┌─────────────┐                                 │
│  │ 文档分割     │  (RecursiveCharacterTextSplitter)│
│  │ chunk_size=500                                │
│  │ chunk_overlap=100                             │
│  └──────┬──────┘                                 │
│         │                                         │
│         ▼                                         │
│  ┌─────────────┐                                 │
│  │ 向量化       │  (DashScopeEmbeddings)           │
│  │ text → vector[1536]                           │
│  └──────┬──────┘                                 │
│         │                                         │
│         ▼                                         │
│  ┌─────────────┐                                 │
│  │ 向量数据库    │  (FAISS / Milvus)               │
│  │ 存储向量+原文 │                                 │
│  └─────────────┘                                 │
│                                                   │
│  ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─    │
│                                                   │
│  查询时：                                          │
│  用户问题 → 向量化 → 检索Top-K相似文档 → 注入Prompt │
│                                                   │
│  Prompt:                                          │
│  "根据以下参考资料回答问题：                        │
│   [相关文档1]                                     │
│   [相关文档2]                                     │
│   问题：{user_question}                           │
│   如果资料中没有答案，请说不知道"                   │
└─────────────────────────────────────────────────┘

workOrderAI/
├── app/
│   ├── api/                    # API 路由层
│   │   ├── classify.py         # 分类接口
│   │   ├── suggest_reply.py    # 回复建议接口
│   │   └── rag_query.py        # RAG 查询接口
│   │
│   ├── services/               # 业务服务层
│   │   ├── classify_service.py # 分类逻辑
│   │   ├── rag_service.py      # RAG 检索逻辑
│   │   └── suggestion_service.py # 回复建议逻辑
│   │
│   ├── chains/                 # LangChain 编排
│   │   ├── classify_chain.py   # 分类链
│   │   ├── rag_chain.py        # RAG 查询链
│   │   └── suggestion_chain.py # 回复建议链
│   │
│   ├── prompts/                # Prompt 模板
│   │   ├── classify_prompt.py
│   │   ├── rag_prompt.py
│   │   └── suggestion_prompt.py
│   │
│   ├── utils/
│   │   ├── config.py           # 配置加载
│   │   └── path_tool.py        # 路径工具
│   │
│   └── main.py                 # FastAPI 入口
│
├── models/
│   └── factory.py              # 模型工厂
│
├── knowledge_base/             # 知识库文档
│   ├── products/               # 产品文档
│   ├── faq/                    # 常见问题
│   └── guides/                 # 操作指南
│
├── vector_store/               # 向量数据库存储
│   └── faiss_index/
│
├── scripts/
│   └── build_vector_store.py   # 构建向量库脚本
│
└── config.yaml                 # 配置文件

第一阶段：基础搭建
├── 1. Python FastAPI 项目骨架
├── 2. 模型工厂初始化（chat/embed/rerank）
└── 3. 配置管理

第二阶段：工单分类
├── 1. 分类 Prompt 设计
├── 2. 分类 API 接口
├── 3. Spring Boot 异步调用集成
└── 4. 回调更新工单字段

第三阶段：RAG 知识库
├── 1. 文档处理脚本（分割+向量化）
├── 2. FAISS 向量库构建
├── 3. RAG 查询链
└── 4. 知识库管理接口

第四阶段：回复建议
├── 1. 回复建议 Prompt
├── 2. 回复建议 API 接口
├── 3. RAG + LLM 生成建议
├── 4. 前端展示 AI 建议
└── 5. 采纳率统计

第五阶段：优化与完善
├── 1. 错误处理与降级策略
├── 2. 缓存机制（减少重复调用）
├── 3. 日志与监控
├── 4. 性能优化（批量向量化等）
└── 5. 单元测试与集成测试